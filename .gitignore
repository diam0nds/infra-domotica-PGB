*.tmp
*.swp
.DS_Store

# Immagini firmware e pacchetti: binari grandi, riscaricabili con hash verificato.
# Nel repo restano il RUNBOOK, il backup della config e gli hash.
upgrade/**/*.bin
upgrade/**/pacchetti/
upgrade/**/target.manifest

# Marcatori di guasto: stato locale, non vanno versionati. Quello che invece
# DEVE finire fuori casa e' hosts/pve/state/replica-casa.txt, che riporta la data
# dell'ultima replica riuscita ed e' l'unico segnale visibile a macchina spenta.
BACKUP-FALLITO.txt
REPLICA-CASA-FALLITA.txt
